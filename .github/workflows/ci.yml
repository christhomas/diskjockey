name: CI

# Push to main validates the trunk post-merge; PRs validate proposed changes.
# Feature-branch pushes do NOT trigger a run on their own — they are covered by
# the PR's `pull_request` run. This avoids the previous double-run, where a
# `push` run and a `pull_request` run fired for the same commit and raced for
# the limited macos-26 runner pool, leaving one of them cancelled.
# No code signing — unit + library tests only.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel an in-progress run when a newer commit supersedes it: keyed per source
# branch for PRs (head_ref), per ref for main pushes (ref_name).
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  test:
    name: Build & Test
    # macos-26 is the Apple Silicon runner for macOS 26.
    # Fall back to macos-15 if the runner isn't available in your plan yet.
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # Auto-select the newest installed Xcode on the runner.
      - name: Select Xcode
        run: |
          XCODE=$(find /Applications -maxdepth 1 -name 'Xcode*.app' -type d 2>/dev/null | sort -V | tail -1)
          echo "Using Xcode: $XCODE"
          sudo xcode-select -s "$XCODE/Contents/Developer"
          xcodebuild -version

      - name: Set up Rust (aarch64)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: stable

      # lib/ output is cached by vendor pin manifest.
      # A cache hit skips all Rust + Go compilation.
      - name: Restore vendor library cache
        id: vendor-cache
        uses: actions/cache@v4
        with:
          path: lib/
          key: vendor-${{ runner.os }}-${{ hashFiles('VENDOR_PINS.txt', 'rust-bundles/**/Cargo.toml', 'rust-bundles/**/Cargo.lock') }}

      # Only restore intermediate Rust artifacts on a vendor cache miss
      # so the next miss still benefits from incremental compilation.
      - name: Restore Cargo cache
        if: steps.vendor-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
            vendor/rust-fs-ext4/target
            vendor/rust-fs-ntfs/target
            vendor/rust-fs-erofs/target
            vendor/rust-fs-squashfs/target
            vendor/rust-img-qcow2/target
            vendor/rust-img-vhd/target
            vendor/rust-img-vhdx/target
            vendor/rust-img-vmdk/target
          key: cargo-${{ runner.os }}-${{ hashFiles('vendor/rust-*/Cargo.lock', 'vendor/rust-*/rust-toolchain.toml') }}
          restore-keys: cargo-${{ runner.os }}-

      # llvm-objcopy is required by build-img-containers.sh to weaken fs_core
      # symbols in the img crates so they don't clash with those in fs_ext4/ntfs.
      # macOS runners do not ship it; install from Homebrew only on a cache miss.
      - name: Install llvm (for objcopy)
        if: steps.vendor-cache.outputs.cache-hit != 'true'
        run: brew install llvm

      - name: Build vendor libraries
        if: steps.vendor-cache.outputs.cache-hit != 'true'
        # vendor-bundles builds the per-extension aggregator staticlibs
        # (lib/bundle_<fs>/) and their C headers, which the FSKit extension
        # targets include via HEADER_SEARCH_PATHS. Without it the Xcode build
        # fails with "'fs_<fs>.h' file not found".
        run: make vendor-fs-ext4 vendor-fs-ntfs vendor-fs-erofs vendor-fs-squashfs vendor-img-containers vendor-gonetworkfs vendor-bundles

      # Same generator the pre-commit hook runs, in check mode. One
      # implementation of "what commit is each library pinned to" lives in
      # chores.yml; this and the hook both forward to it.
      # scripts/check-vendor-pins.sh is superseded and nothing calls it.
      #
      # NOTE: this runner has no `chore` install step, and the vendor build
      # step above already goes through the Makefile, which needs one too.
      # Adding it is a separate decision from this change.
      - name: Validate vendor pins
        run: chore pins:check

      - name: Install xcbeautify
        run: brew install xcbeautify

      # UI tests are excluded — they require a running app and virtual display.
      - name: Test
        run: |
          set -o pipefail
          xcodebuild test \
            -project DiskJockey.xcodeproj \
            -scheme DiskJockey \
            -destination 'platform=macOS,arch=arm64' \
            -skip-testing DiskJockeyUITests \
            ONLY_ACTIVE_ARCH=YES \
            ARCHS=arm64 \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_ALLOWED=NO \
          | xcbeautify --renderer github-actions
